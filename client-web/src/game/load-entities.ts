import { engine } from "./app";

// Initial loading screen entities
export const initialLoadingScreen = engine.createEntity();
export const loadingBar = engine.createEntity();

// Main screen entities
export const mainScreen = engine.createEntity();
export const logo = engine.createEntity();
export const background = engine.createEntity();

// Game-mode entities
export const gameScreen = engine.createEntity();
