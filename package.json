{
  "name": "thirteen",
  "version": "0.0.1",
  "description": "The classic card game Thirteen",
  "scripts": {
    "db:reset:test": "npm run db:drop:test --force && npm run db:create:test && npm run  db:migrate:test",
    "db:drop:test": "(cd server; NODE_ENV=test sequelize db:drop)",
    "db:create:test": "(cd server; NODE_ENV=test sequelize db:create)",
    "db:migrate:test": "(cd server; NODE_ENV=test sequelize db:migrate)",
    "test": "jest"
  },
  "author": "Ryan Poe",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "@types/node": "^12.6.2",
    "jest": "^24.8.0",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.3"
  },
  "dependencies": {
    "lodash.clonedeep": "^4.5.0"
  }
}
